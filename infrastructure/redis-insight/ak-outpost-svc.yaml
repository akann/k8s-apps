apiVersion: v1
kind: Service
metadata:
  name: ak-outpost-redis-insight
  namespace: redis-insight
spec:
  type: ExternalName
  externalName: ak-outpost-redis-insight.authentik.svc.cluster.local
  ports:
    - port: 9000
      targetPort: 9000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ak-outpost-redis-insight
  namespace: redis-insight
  annotations:
    nginx.ingress.kubernetes.io/proxy-buffer-size: "512k"
spec:
  ingressClassName: nginx
  rules:
    - host: redis.yanatech.co.uk
      http:
        paths:
          - path: /outpost.goauthentik.io
            pathType: Prefix
            backend:
              service:
                name: ak-outpost-redis-insight
                port:
                  number: 9000
  tls:
    - hosts:
        - redis.yanatech.co.uk
      secretName: wildcard-yanatech-tls
