apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cilium
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  source:
    repoURL: https://helm.cilium.io
    chart: cilium
    targetRevision: 1.18.11
    helm:
      valuesObject:
        routingMode: native
        autoDirectNodeRoutes: true
        ipv4NativeRoutingCIDR: 10.244.0.0/16
        devices: eth0
        kubeProxyReplacement: true
        # HA apiserver access (Cilium 1.18+): client-side failover across all three
        # control planes. Replaces the old k8sServiceHost pinned to k8s-cp-1, which
        # crash-looped every agent (re)start while cp-1 was down (seen 2026-07-02
        # during the pve1 reboot). Node IPs are already in the apiserver cert SANs.
        k8s:
          apiServerURLs: "https://192.168.33.21:6443 https://192.168.33.22:6443 https://192.168.33.23:6443"
        ipam:
          mode: cluster-pool
          operator:
            clusterPoolIPv4PodCIDRList:
              - 10.244.0.0/16
        hubble:
          enabled: true
          relay:
            enabled: true
          ui:
            enabled: true
            ingress:
              enabled: true
              className: nginx
              annotations:
                nginx.ingress.kubernetes.io/auth-url: "https://hubble.yanatech.co.uk/outpost.goauthentik.io/auth/nginx"
                nginx.ingress.kubernetes.io/auth-signin: "https://hubble.yanatech.co.uk/outpost.goauthentik.io/start?rd=$escaped_request_uri"
                nginx.ingress.kubernetes.io/auth-response-headers: "Set-Cookie,X-authentik-username,X-authentik-groups,X-authentik-email,X-authentik-name,X-authentik-uid"
                nginx.ingress.kubernetes.io/auth-snippet: |
                  proxy_set_header X-Original-URL $scheme://$http_host$request_uri;
                nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
                nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
                nginx.ingress.kubernetes.io/proxy-http-version: "1.1"
                nginx.ingress.kubernetes.io/configuration-snippet: |
                  proxy_set_header Upgrade $http_upgrade;
                  proxy_set_header Connection "upgrade";
              hosts:
                - hubble.yanatech.co.uk
              tls:
                - secretName: wildcard-yanatech-tls
                  hosts:
                    - hubble.yanatech.co.uk
        operator:
          replicas: 2
        bpf:
          masquerade: true
        prometheus:
          enabled: true
          serviceMonitor:
            enabled: true
            labels:
              release: kube-prometheus-stack
  destination:
    server: https://kubernetes.default.svc
    namespace: kube-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
