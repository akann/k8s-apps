# Topic names here MUST match the dotted names in yana-stocks'
# packages/kafka-client/src/topics.ts exactly (KAFKA_TOPICS.*) — that
# package is what every producer/consumer actually connects with, and it is
# the source of truth for the real topic name, not this file.
#
# These previously used hyphenated names (stocks-prices-raw, etc.) that
# didn't match the app code at all. Kafka auto-created the real dotted-name
# topics on first use with 1 partition and no explicit retention config,
# while these KafkaTopic resources quietly managed a completely different,
# never-produced-to, never-consumed-from set of topics for the whole time
# they existed. Confirmed live (2026-07-16) via `kafka-topics.sh --describe`
# and `kafka-consumer-groups.sh --describe` against every consumer group
# (email-service, profile-service, portfolio-api, price-processor,
# portfolio-service): all real traffic is on the dotted names, all on
# partition 0, zero consumers of the old hyphenated names.
#
# `partitions: 1` below matches the CURRENT live partition count, not the
# `3` these topics were originally intended to have — every producer keys
# its messages (price-ingestor by symbol, auth-service by userID), so
# bumping partition count is a live rebalance of the key→partition mapping,
# not a free operation. Strimzi's topic operator adopts an existing topic
# in place without deleting it as long as spec.partitions doesn't ask for a
# decrease, so this fix only takes over configuration (retention/cleanup)
# — it deliberately does NOT attempt the partition increase needed to
# actually fix KEDA's autoscaling ceiling (every one of these topics can
# only ever have 1 effective consumer per group right now). That's a
# separate, deliberate follow-up requiring coordination with each
# consumer's ordering assumptions, not bundled into this fix.
---
apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: stocks.prices.raw
  namespace: kafka
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  partitions: 1
  replicas: 3
  config:
    retention.ms: 86400000      # 24 hours
    cleanup.policy: delete
---
apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: stocks.prices.processed
  namespace: kafka
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  partitions: 1
  replicas: 3
  config:
    retention.ms: 604800000     # 7 days
    cleanup.policy: delete
---
apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: stocks.signals.sentiment
  namespace: kafka
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  partitions: 1
  replicas: 3
  config:
    retention.ms: 604800000     # 7 days
    cleanup.policy: delete
---
apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: stocks.signals.prediction
  namespace: kafka
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  partitions: 1
  replicas: 3
  config:
    retention.ms: 604800000     # 7 days
    cleanup.policy: delete
---
apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: users.registered
  namespace: kafka
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  partitions: 1
  replicas: 3
  config:
    retention.ms: 604800000     # 7 days
    cleanup.policy: delete
---
apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: stocks.portfolio.events
  namespace: kafka
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  partitions: 1
  replicas: 3
  config:
    retention.ms: 2592000000    # 30 days
    cleanup.policy: delete
