# Topic names here MUST match the dotted names in yana-stocks'
# packages/kafka-client/src/topics.ts exactly (KAFKA_TOPICS.*) — that
# package is what every producer/consumer actually connects with, and it is
# the source of truth for the real topic name, not this file.
#
# These previously used hyphenated names (stocks-prices-raw, etc.) that
# didn't match the app code at all. Kafka auto-created the real dotted-name
# topics on first use with 1 partition and no explicit retention config,
# while these KafkaTopic resources quietly managed a completely different,
# never-produced-to, never-consumed-from set of topics for the whole time
# they existed. Confirmed live (2026-07-16) via `kafka-topics.sh --describe`
# and `kafka-consumer-groups.sh --describe` against every consumer group
# (email-service, profile-service, portfolio-api, price-processor,
# portfolio-service): all real traffic is on the dotted names, all on
# partition 0, zero consumers of the old hyphenated names.
#
# `partitions: 1` below (except users.registered, see its own comment)
# matches the CURRENT live partition count, not the `3` these topics were
# originally intended to have — every producer keys its messages
# (price-ingestor/price-processor/sentiment-analyzer/ml-predictor by
# symbol, auth-service by userID, portfolio-service by portfolioId), so
# bumping partition count changes the live key→partition mapping. Strimzi's
# topic operator supports increasing (never decreasing) an existing
# topic's partition count in place. The safe precondition, confirmed fresh
# immediately before each bump: every consumer group's lag is 0 — with
# nothing backlogged, there's no in-flight message racing the new hash
# assignment on a different partition, which is the actual risk this
# guards against. users.registered was bumped first as the pilot (lowest
# risk: single event per user, no ongoing per-key derived state); the rest
# follow once that validated cleanly.
---
apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: stocks.prices.raw
  namespace: kafka
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  partitions: 1
  replicas: 3
  config:
    retention.ms: 86400000      # 24 hours
    cleanup.policy: delete
---
apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: stocks.prices.processed
  namespace: kafka
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  partitions: 1
  replicas: 3
  config:
    retention.ms: 604800000     # 7 days
    cleanup.policy: delete
---
apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: stocks.signals.sentiment
  namespace: kafka
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  partitions: 1
  replicas: 3
  config:
    retention.ms: 604800000     # 7 days
    cleanup.policy: delete
---
apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: stocks.signals.prediction
  namespace: kafka
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  partitions: 1
  replicas: 3
  config:
    retention.ms: 604800000     # 7 days
    cleanup.policy: delete
---
apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: users.registered
  namespace: kafka
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  # Bumped 1 -> 3 (2026-07-16) as the pilot for the partition-increase
  # follow-up noted above — confirmed consumer group lag=0 immediately
  # before this change, and this topic is keyed by userID with no ongoing
  # per-user derived state (a single registration event per user), making
  # it the lowest-risk candidate to validate the pattern on first.
  partitions: 3
  replicas: 3
  config:
    retention.ms: 604800000     # 7 days
    cleanup.policy: delete
---
apiVersion: kafka.strimzi.io/v1
kind: KafkaTopic
metadata:
  name: stocks.portfolio.events
  namespace: kafka
  labels:
    strimzi.io/cluster: kafka-cluster
spec:
  partitions: 1
  replicas: 3
  config:
    retention.ms: 2592000000    # 30 days
    cleanup.policy: delete
