apiVersion: v1
kind: Service
metadata:
  name: price-processor
  namespace: yana-stocks
  labels:
    app: price-processor
spec:
  selector:
    app: price-processor
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
