# Pulls JWT_SECRET from Infisical and creates a Kong HS256 JWT credential.
# Kong matches the 'iss: "yana-stocks"' claim in JWTs issued by auth-service
# to this credential and verifies the HS256 signature with the shared secret.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: auth-service-jwt-credential
  namespace: yana-stocks
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: infisical
    kind: ClusterSecretStore
  target:
    name: auth-service-jwt-credential
    creationPolicy: Owner
    template:
      metadata:
        labels:
          konghq.com/credential: jwt
      data:
        kongCredType: jwt
        algorithm: HS256
        key: "yana-stocks"
        secret: "{{ .jwt_secret }}"
  data:
    - secretKey: jwt_secret
      remoteRef:
        key: /yana-stocks/auth-service/JWT_SECRET
