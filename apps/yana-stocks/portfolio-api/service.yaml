apiVersion: v1
kind: Service
metadata:
  name: portfolio-api
  namespace: yana-stocks
  labels:
    app: portfolio-api
spec:
  selector:
    app: portfolio-api
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
