apiVersion: apps/v1
kind: Deployment
metadata:
  name: portfolio-api
  namespace: yana-stocks
  labels:
    app: portfolio-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: portfolio-api
  template:
    metadata:
      labels:
        app: portfolio-api
    spec:
      containers:
        - name: portfolio-api
          image: harbor.yanatech.co.uk/yana-stocks/portfolio-api:latest
          ports:
            - containerPort: 3000
          envFrom:
            - secretRef:
                name: portfolio-api-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 300m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 15
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 30
            periodSeconds: 30
