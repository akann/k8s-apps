apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: auth-service-pg-backup
  namespace: yana-stocks
spec:
  schedule: "0 1 * * *"
  backupOwnerReference: self
  cluster:
    name: auth-service-pg
