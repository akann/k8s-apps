apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: ml-predictor
  namespace: yana-stocks
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: ml-predictor
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
