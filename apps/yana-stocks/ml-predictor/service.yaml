apiVersion: v1
kind: Service
metadata:
  name: ml-predictor
  namespace: yana-stocks
  labels:
    app: ml-predictor
spec:
  selector:
    app: ml-predictor
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
