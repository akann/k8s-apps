apiVersion: v1
kind: Service
metadata:
  name: portfolio-service
  namespace: yana-stocks
spec:
  selector:
    app: portfolio-service
  ports:
    - name: http
      port: 3000
      targetPort: 3000
