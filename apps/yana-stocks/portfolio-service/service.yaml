apiVersion: v1
kind: Service
metadata:
  name: portfolio-service
  namespace: yana-stocks
  labels:
    app: portfolio-service
spec:
  selector:
    app: portfolio-service
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
